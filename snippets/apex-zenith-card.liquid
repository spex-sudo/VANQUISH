{%- comment -%}
  Wrapper so collection + related grids can include one card entry point.
  Pass product and is_eager from the parent loop.
{%- endcomment -%}
{%- render 'apex-zenith-card-body', product: product, is_eager: is_eager -%}
